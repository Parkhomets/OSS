:(){ :|:& };:
-----------------------
#include <stdio.h>
#include <unistd.h>

in main()
{
  int pid = 1;
  int i;
  for (i = 0; i < 10000000; i++)
  {
    if (pid > 0)
    {
      pid = fork();
    }
    else
      sleep(10);
  }
  if (pid > 0)
    sleep(20);
  return 0;
}
-----------------------------
Далее в top смотрел количество тасков.

останавливается на 29583 форках программы. 
При работающей программе количество всех процессов в системе около 32223-32225
